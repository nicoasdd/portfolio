---
description: Project Development Guidelines
globs: ["**/*"]
alwaysApply: true
---

# portfolio Development Guidelines

Auto-generated from all feature plans. Last updated: 2026-04-24

## Active Technologies
- TypeScript 5.x (Node.js 20 LTS) — same as existing projec + Astro 5.x, Tailwind CSS 4.x (via `@tailwindcss/vite`), Astro Content Collections (Markdown + Zod), `@astrojs/sitemap`. No new runtime dependencies introduced by this feature. (002-about-section)
- Filesystem — single Markdown file at `src/content/about/profile.md`; profile photo and optional resume PDF under `public/about/`. (002-about-section)
- TypeScript 5.x on Node.js 20 LTS (already in `.nvmrc`) + Astro 5.x, Tailwind CSS 4.x, `@astrojs/sitemap`, Zod (no new dependencies introduced by this feature) (003-template-mode)
- Markdown files under `src/content/`, validated by Zod schemas in `src/content.config.ts` (no change) (003-template-mode)
- Cursor agent skill (Markdown prompt at `.cursor/skills/optimize-project-from-resume/`); zero new runtime dependencies — uses only built-in agent tools (Read for `.md`/`.pdf` resumes, Glob, Grep, StrReplace, Shell). (004-optimize-project-skill)
- Filesystem only — reads a user-provided resume path and edits existing project files under `src/content/projects/**/*.md` in place. (004-optimize-project-skill)
- TypeScript 5.x on Node.js 20 LTS (pinned in `.nvmrc`, matches existing project) + Astro 5.x, Tailwind CSS 4.x (via `@tailwindcss/vite`), Astro Content Collections + Zod, `@astrojs/sitemap`. No new runtime dependencies. (005-blueprint-redesign)
- Filesystem — all content in `src/content/` as Markdown validated by Zod schemas in `src/content.config.ts`; theme preference in browser `localStorage` under a single key. (005-blueprint-redesign)

- TypeScript 5.x (Node.js 20 LTS) + Astro 5.x, Tailwind CSS 4.x, `@astrojs/tailwind`, `@astrojs/sitemap`, Astro Content Collections (Markdown + Zod schemas) (001-portfolio-showcase)

## Project Structure

```text
backend/
frontend/
tests/
```

## Commands

npm test && npm run lint

## Code Style

TypeScript 5.x (Node.js 20 LTS): Follow standard conventions

## Recent Changes
- 005-blueprint-redesign: Added TypeScript 5.x on Node.js 20 LTS (pinned in `.nvmrc`, matches existing project) + Astro 5.x, Tailwind CSS 4.x (via `@tailwindcss/vite`), Astro Content Collections + Zod, `@astrojs/sitemap`. No new runtime dependencies.
- 004-optimize-project-skill: Added a Cursor agent skill at `.cursor/skills/optimize-project-from-resume/` that rewrites narrative segments of project entries to match the user's current career stage as derived from their resume. Zero new runtime dependencies; uses built-in agent tools only.
- 003-template-mode: Added TypeScript 5.x on Node.js 20 LTS (already in `.nvmrc`) + Astro 5.x, Tailwind CSS 4.x, `@astrojs/sitemap`, Zod (no new dependencies introduced by this feature)


<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
